name: erfanbanaei
description: "Hi, I'm erfanbanari, a computer engineer passionate about building innovative tech solutions."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  timeline_tile: ^2.0.0
  url_launcher: ^6.3.0
  auto_size_text: ^3.0.0
  scrollable_positioned_list: ^0.3.8
  google_nav_bar: ^5.0.6
  flutter_map_cancellable_tile_provider: ^3.0.2
  flutter_map: ^8.0.0
  latlong2: ^0.9.1
  http: ^1.2.2
  flutter_animate: ^4.5.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
# test
flutter_web:
  optimization: true
  source_maps: false

flutter:
  uses-material-design: true
  assets:
    - assets/ss/
    - assets/
    - assets/emoji/
    - assets/svg/
